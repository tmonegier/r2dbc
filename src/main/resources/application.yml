spring:
  liquibase:
    url: jdbc:postgresql://localhost:5432/reactive_examples_db
    user: postgres
    password: admin
  datasource:
    url: jdbc:postgresql://localhost:5432/reactive_examples_db
    user: postgres
    password: admin
    
logging:
  level:
    org:
      springframework:
        data:
          r2dbc: DEBUG